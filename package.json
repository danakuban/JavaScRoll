{
  "name": "sharedb-example-rich-text",
  "version": "1.0.0",
  "description": "A simple rich-text editor example based on Quill and ShareDB",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p static/dist/ && ./node_modules/.bin/browserify *.js -o static/dist/bundle.js",
    "test": "cd roleFramework && mocha --reporter spec",
    "start": "node server.js",
    "buildAndStart": "npm run build && npm run start"
  },
  "author": "Nate Smith",
  "contributors": [
    "Avital Oliver <avital@aoliver.org> (https://aoliver.org/)"
  ],
  "license": "MIT",
  "dependencies": {
    "@teamwork/websocket-json-stream": "^2.0.0",
    "chai": "^4.2.0",
    "deltajs": "0.0.6",
    "dom-parser": "^0.1.6",
    "express": "^4.17.1",
    "mocha": "^7.1.1",
    "reconnecting-websocket": "^4.2.0",
    "sharedb": "^1.0.0-beta",
    "ws": "^7.2.0",
    "xml": "^1.0.1",
    "ot-json0": "latest"
  },
  "devDependencies": {
    "browserify": "^16.5.0"
  }
}
